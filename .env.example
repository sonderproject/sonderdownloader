# Required for reliable extraction. Sign up free at
# https://app.scrapingbee.com/register (1000 credits/mo). This routes
# Zillow requests through a residential-IP proxy pool with PerimeterX
# bypass — the reliable path from Vercel's blocked datacenter IPs.
SCRAPINGBEE_API_KEY=

# Alt scraping services — pick any one. Set whichever you already use.
# ZENROWS_API_KEY=
# SCRAPERAPI_KEY=
